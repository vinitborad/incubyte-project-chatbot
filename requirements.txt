# Core server dependencies
fastapi==0.104.1
uvicorn==0.24.0
pymongo==4.6.0
python-dotenv==1.0.0
pydantic==2.5.0
requests==2.31.0

# LangChain dependencies for core logic
langchain-core==0.1.16
langchain-openai==0.0.5
langchain-community==0.0.13
langgraph==0.0.20

# Redis for chat history
# Note: Using <5.0 to maintain compatibility and avoid connection errors.
redis<5.0
